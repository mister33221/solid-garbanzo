version: '3.8'

services:
  redis:
    image: redis
    container_name: my-redis
    ports:
      - "6379:6379"

  redisinsight :
    image: redislabs/redisinsight
    container_name: my-redisinsight
    ports:
      - "8001:8001"
    depends_on:
      - redis
